# 2026-08-21

- 通读 workInfoNeu 项目全部源码，输出架构与模块分析。
- 项目要点：Neutralinojs 6.9 桌面壳 + Vue3/Pinia/Element Plus/TS/Vite 前端的工作日志应用。
- 前端构建到 resources/，无独立后端进程；业务 API 由 src/renderer/src/neu/neuBackend.ts 在渲染进程内注入 window.api（Electron preload 形状兼容层），底层用 Neutralino filesystem API 读写 JSON 文件（logs/settings/reminders），原子写（tmp+move）+ 串行写链。
- 节假日数据内置 2026（官方）/2027（推算）两份 JSON，随资源 fetch 加载。
- 导出：Markdown 纯文本 + ExcelJS（vendor 全局脚本）生成 xlsx，走系统保存对话框。
- 提醒：渲染进程 setInterval 60s 轮询，工作日 18:00 后系统通知，按日期去重。
- 打包：scripts/package.cjs 手动打包（asar 生成 resources.neu + 复制 exe + zip），绕开 neu CLI 问题；neu-run.cjs 指向全局安装的 @neutralinojs/neu。

- 新增附件上传功能（按月 × 5 分类：week1/week2/week3/week4/monthly）。
- 改动：types.ts 加 Attachment/AttachmentCategory/ATTACHMENT_CATEGORIES；ipc.ts 加 attachment:getMonth/upload/delete 与 api.attachment。
- 新增 src/renderer/src/neu/attachments.ts（持久层：磁盘 <dataDir>/attachments/{YYYY-MM}/{dir}/{uuid}__{name} + attachments.json，原子写 + 串行写链）。
- 新增 src/renderer/src/stores/attachmentStore.ts（Pinia，buckets/loaded/uploading 三 map）。
- 新增 src/renderer/src/components/editor/AttachmentUploader.vue（多文件 input + 列表 + 删除 + 成功 ElMessage）。
- 集成点：CalendarView 右侧栏在 LogEditor 下方加附件区，5 个分类同时渲染。
- 无大小/类型校验，纯透传 ArrayBuffer；FileReader via f.arrayBuffer() 在 WebView2 可用。
- vue-tsc 通过；vite build 正常（构建时 resources/ 已在产物里有内容，沙箱 safe-delete 拒绝 emptyOutDir 与代码无关）。

- 修复上传失败 bug：Windows 下 os.getPath('data') 返回反斜杠路径（实测为 C:\Users\Dawn-PC\AppData\Roaming），原 ensureDir 按 '/' 切分拼出 '/C:\...' 非法路径导致目录创建静默失败、writeBinaryFile 落空目录报错。
- 修复方案：attachments.ts 新增 baseDir() 统一反斜杠转正斜杠；ensureDir 先归一化分隔符再逐级创建，盘符开头不加前导斜杠；upload 失败时回滚已写入文件。
- 本机应用数据目录：C:\Users\Dawn-PC\AppData\Roaming\workInfoNeu\data\（Neutralino 'data' 映射到 Roaming 而非 Local）。

- 增强附件功能：
  1) 新增 utils/attachmentWeek.ts：按 (category, year, month) 算出该分类对应的工作日范围，格式 "M.DD-M.DD（工作日）"，复用 holidays.isWorkday。
  2) 新增 downloadFile 持久层 + window.api.attachment.download：前端 Blob+URL.createObjectURL 触发下载。
  3) AttachmentUploader 头部状态徽章：已上传显示绿色 "已完成 8.10-8.14（工作日）"，未上传显示灰色 "未上传"；整个分类块上传完成后浅绿描边。
  4) 文件项加 Download 图标按钮，Download/Delete 并列。
  5) 单测覆盖：跨年(2027-01)、2 月平年首日是周日、含法定假日的 10 月，国庆 week4 等均通过。
